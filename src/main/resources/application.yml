spring:
  applicaton:
    name: admin-service
  cloud:
    kubernetes:
      discovery:
        namespace: carloszamo077-2-dev
        service-labels:
          spring-boot: true
      client:
        namespace: carloszamo077-2-dev